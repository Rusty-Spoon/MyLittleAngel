using UnityEngine;
using System.Collections;

public class controlMenu : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
		if (Input.GetKey("escape")
		    ||
		    Input.GetKey("return")
		    ||
		    Input.GetKey("space"))
		{
			Application.LoadLevel("SewerScene");
		}
	}
}